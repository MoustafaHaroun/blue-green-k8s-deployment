apiVersion: apps/v1
kind: Deployment
metadata:
  name: basic-web-app-deployment 
spec:
  replicas: 2
  selector:
    matchLabels:
      app: basic-web-app 
  template:
    metadata:
      labels:
        app: basic-web-app 
    spec:
      containers:
      - name: web-app 
        image: europe-west4-docker.pkg.dev/public-cloud-concepts-471510/basic-web--app/basic-web-app:latest
        ports:
        - containerPort: 80 
